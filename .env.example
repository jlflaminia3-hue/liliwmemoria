APP_NAME=Laravel
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=liliwmemoria.com
APP_TIMEZONE=Asia/Manila

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=liliwmemoria
DB_USERNAME=liliwmemoria
DB_PASSWORD=liliwmemoria

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_ENCRYPTION=tls
MAIL_USERNAME=liliwmemoria@gmail.com
MAIL_PASSWORD=iuvmxbaxmycnungy
MAIL_FROM_ADDRESS="liliwmemoria@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

# Included in the payment schedule email sent from the Payments module.
# You can use multi-line values.
PAYMENT_INSTRUCTIONS="Please pay your monthly installment on or before the due date.\n\nPayment options:\n- Cash (office)\n- Bank transfer\n- GCash\n\nUse your Payment Plan Number as reference."

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

MASTER_ADMIN_NAME="Master Admin"
MASTER_ADMIN_EMAIL=jlflaminia12@gmail.com
MASTER_ADMIN_PASSWORD=jldlwlrma

# Cemetery map overlay (used by admin lot map + public/visitor locator)
# Path is relative to `public/`
CEMETERY_MAP_IMAGE="backend/assets/images/map_visitor.jpg"

# Optional: walking lanes JSON used by the visitor tomb locator (relative to `public/`)
# CEMETERY_LANES_JSON="backend/assets/lanes.json"
